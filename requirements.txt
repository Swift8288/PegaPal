# PegaPal — Dependencies (Streamlit Cloud compatible)
# Total cost: $0

# ── Core ──
streamlit>=1.35.0
python-dotenv>=1.0.1

# ── Vector Database ──
chromadb>=0.4.22
pysqlite3-binary>=0.5.2    # newer SQLite for ChromaDB on Streamlit Cloud
protobuf>=3.20,<5

# ── LLM Clients ──
groq>=0.5.0
anthropic>=0.25.0
httpx>=0.27.0

# ── Document Parsing (community uploads) ──
pdfplumber>=0.10.3
python-docx>=1.1.0

# ── HTTP (crawler & API calls) ──
requests>=2.31.0
beautifulsoup4>=4.12.3
